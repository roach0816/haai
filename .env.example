# Optional Docker Compose host port override.
# HAAI_HTTP_PORT=8787

# Set true when HAAI is behind a trusted reverse proxy that sends forwarded headers.
# HAAI_TRUST_PROXY=false

# Optional file-backed bootstrap secret for deterministic encryption key setup.
# HAAI_SECRET_FILE=./docker/secrets/haai_secret
